name: Publish

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'
          scope: '@dsplayground-analytics'

      - name: Install
        run: npm ci
        env:
          HUSKY: 0

      - name: Test + build
        run: |
          npm run test
          npm run build

      - name: Verificar artefatos dist
        run: |
          ls dist/index.js dist/index.cjs dist/index.d.ts dist/sdk.umd.js || exit 1

      - name: Publish no npmjs.com
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@dsplayanalytics'

      - name: Publish no GitHub Packages
        run: |
          npm pkg set name="@dsplayanalytics/sdk"
          npm publish --access public
          npm pkg set name="@dsplayground-analytics/sdk"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Criar GitHub Release com bundle UMD anexado
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF##*/}"
          gh release view "$tag" >/dev/null 2>&1 \
            || gh release create "$tag" --title "$tag" --notes "Ver CHANGELOG.md"
          gh release upload "$tag" dist/sdk.umd.js dist/sdk.umd.js.map --clobber
